<h1>Listing amus</h1>

<table>
  <thead>
    <tr>
      <th>Cute</th>
      <th>Schedule</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @amus.each do |amu| %>
      <tr>
        <td><%= amu.cute %></td>
        <td><%= amu.schedule %></td>
        <td><%= link_to 'Show', amu %></td>
        <td><%= link_to 'Edit', edit_amu_path(amu) %></td>
        <td><%= link_to 'Destroy', amu, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Amu', new_amu_path %>
