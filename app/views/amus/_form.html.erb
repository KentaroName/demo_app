<%= form_for(@amu) do |f| %>
  <% if @amu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@amu.errors.count, "error") %> prohibited this amu from being saved:</h2>

      <ul>
      <% @amu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cute %><br>
    <%= f.text_field :cute %>
  </div>
  <div class="field">
    <%= f.label :schedule %><br>
    <%= f.number_field :schedule %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
